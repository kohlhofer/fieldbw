---
import BaseLayout from '../layouts/BaseLayout.astro';

const projects = [
  {
    name: 'Note Taker',
    description: 'Private, on-device meeting notes with AI.',
    url: '/note-taker',
    type: 'macOS App',
    comingSoon: true
  },
  {
    name: 'MapsAll',
    description: 'Mind-mapping powered by AI — ideas made spatial.',
    url: 'https://www.mapsall.com',
    type: 'Web App'
  },
  {
    name: 'DXC4',
    description: 'Finding structure in chess games and data',
    url: 'https://dxc4.com',
    type: 'Web App'
  },
  {
    name: 'Rock or Pop',
    description: 'Create and share mixtapes',
    url: 'https://rockorpop.com/',
    type: 'Web App'
  },
  {
    name: 'MIDI Volume Control',
    description: 'Smooth, tactile sound control for macOS.',
    url: 'http://kohlhofer.com/midiVol/',
    type: 'macOS App'
  }
];
---

<BaseLayout
  backgroundImage="/images/bg1.jpeg"
  gradientOverlay="linear-gradient(to bottom, rgba(0, 0, 0, 0.1), rgba(0, 0, 0, 0.3) 50%, rgba(0, 0, 0, 0.8))"
>
  <div class="relative min-h-screen flex flex-col">

    <!-- Top Navigation -->
    <nav class="relative z-10 px-12 py-6 flex justify-end">
      <a
        href="/about"
        class="text-white/80 hover:text-white transition-all duration-200 text-sm tracking-wide px-4 py-2 rounded-full border border-white/20 backdrop-blur-sm bg-white/[0.05] hover:bg-white/[0.08] hover:border-white/30"
      >
        About →
      </a>
    </nav>

    <!-- Center Content -->
    <div class="relative z-10 flex-1 flex items-center px-12">
      <div class="text-left md:pl-8">
        <h1 class="text-5xl md:text-7xl font-light text-white tracking-wide leading-none">
          <div>Field</div>
          <div>Bureau <span class="text-white/90">+</span></div>
          <div>Werkstatt</div>
        </h1>
      </div>
    </div>

    <!-- Bottom Project Links -->
    <div class="relative z-10 px-12 py-8">
      <div class="flex flex-wrap gap-y-8 w-full max-w-7xl [&>*:nth-child(3n+1)]:md:border-l-0 [&>*:nth-child(3n+1)]:lg:border-l [&>*:nth-child(5n+1)]:lg:border-l-0">
        {projects.map((project, index) => (
          project.comingSoon ? (
            <div
              class="cursor-default animate-fade-in-up flex-shrink-0 w-full md:w-1/3 lg:w-1/5 pt-8 md:pt-0 border-t md:border-t-0 md:border-l border-white/20 md:px-8"
              style={`animation-delay: ${index * 150}ms; opacity: 0;`}
            >
              <div class="mb-2 flex items-center gap-2">
                <span class="text-white/40 text-[10px] uppercase tracking-wider">{project.type}</span>
                <span class="text-white/40 text-[10px] uppercase tracking-wider">• Coming Soon</span>
              </div>
              <h3 class="text-white/60 font-medium text-base mb-2 drop-shadow-lg">
                {project.name}
              </h3>
              <p class="text-white/50 text-xs leading-relaxed drop-shadow-lg">
                {project.description}
              </p>
            </div>
          ) : (
            <a
              href={project.url}
              target="_blank"
              rel="noopener noreferrer"
              class="group transition-all duration-200 animate-fade-in-up flex-shrink-0 w-full md:w-1/3 lg:w-1/5 pt-8 md:pt-0 border-t md:border-t-0 md:border-l border-white/20 md:px-8 hover:backdrop-blur-sm hover:bg-white/[0.03]"
              style={`animation-delay: ${index * 150}ms; opacity: 0;`}
            >
              <div class="mb-2">
                <span class="text-white/40 text-[10px] uppercase tracking-wider group-hover:text-white/60 transition-colors">{project.type}</span>
              </div>
              <h3 class="text-white font-medium text-base mb-2 drop-shadow-lg group-hover:text-white transition-colors">
                {project.name}
              </h3>
              <p class="text-white/70 text-xs leading-relaxed drop-shadow-lg group-hover:text-white/90 transition-colors">
                {project.description}
              </p>
            </a>
          )
        ))}
      </div>

      <!-- Footer -->
      <div class="mt-8 max-w-7xl mx-auto">
        <p class="text-center text-white/30 text-xs tracking-wide">
          Field Bureau <span class="text-white/20">+</span> Werkstatt
          <span class="mx-2 text-white/20">•</span>
          © {new Date().getFullYear()} Field Bureau & Werkstatt LLC. All rights reserved.
        </p>
      </div>
    </div>
  </div>
</BaseLayout>
